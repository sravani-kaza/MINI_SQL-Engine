To run the script:
python3 20171189.py "Query"


files folder must contain .csv files contaiing data and metadata.txt with metadata of tables present

processor.py contains the query class that contains all the functions for execution.